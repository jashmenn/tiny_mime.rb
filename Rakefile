require 'config/requirements'
require 'config/hoe' # setup Hoe + all gem configuration
require 'opc-gems-tasks' rescue nil # for internal use only

Dir['tasks/**/*.rake'].each { |rake| load rake }

